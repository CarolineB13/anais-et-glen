<section id="hero" class="hero">
  <img
    src="/images/decorations/deco1.png"
    alt=""
    class="hero__flowers hero__flowers--top"
    aria-hidden="true"
  />

  <img
    src="/images/decorations/deco5.png"
    alt=""
    class="hero__flowers hero__flowers--bottom"
    aria-hidden="true"
  />

  <div class="hero__frame">
    <div class="hero__content">
      <p class="hero__kicker">Nous avons une merveilleuse nouvelle…</p>

      <h1 class="hero__names">
        <span>Anaïs</span>

        <span class="hero__ampersand"> & </span>

        <span>Glen</span>
      </h1>

      <p class="hero__announcement">vont se dire oui</p>

      <div class="hero__ornament" aria-hidden="true">
        <span></span>
        <i>♡</i>
        <span></span>
      </div>

      <p class="hero__intro">
        Nous serions heureux de vous avoir à nos côtés pour célébrer cette
        journée.
      </p>

      <div class="countdown" aria-label="Compte à rebours avant le mariage">
        <div class="countdown__item">
          <strong id="days">000</strong>
          <span>jours</span>
        </div>

        <div class="countdown__item">
          <strong id="hours">00</strong>
          <span>heures</span>
        </div>

        <div class="countdown__item">
          <strong id="minutes">00</strong>
          <span>minutes</span>
        </div>

        <div class="countdown__item">
          <strong id="seconds">00</strong>
          <span>secondes</span>
        </div>
      </div>

      <p class="hero__date">Samedi · 19 juin · 2027</p>

      <div class="hero__actions">
        <a href="#programme" class="hero__button hero__button--primary">
          Découvrir la journée
        </a>

        <a href="#rsvp" class="hero__button hero__button--secondary">
          Confirmer ma présence
        </a>
      </div>
    </div>
  </div>
</section>

<script>
  const weddingDate = new Date("2027-06-19T14:30:00").getTime();

  const days = document.getElementById("days");
  const hours = document.getElementById("hours");
  const minutes = document.getElementById("minutes");
  const seconds = document.getElementById("seconds");

  function updateCountdown() {
    const distance = weddingDate - Date.now();

    if (distance <= 0) {
      days.textContent = "000";
      hours.textContent = "00";
      minutes.textContent = "00";
      seconds.textContent = "00";
      return;
    }

    const day = Math.floor(distance / 86400000);

    const hour = Math.floor((distance % 86400000) / 3600000);

    const minute = Math.floor((distance % 3600000) / 60000);

    const second = Math.floor((distance % 60000) / 1000);

    days.textContent = String(day).padStart(3, "0");

    hours.textContent = String(hour).padStart(2, "0");

    minutes.textContent = String(minute).padStart(2, "0");

    seconds.textContent = String(second).padStart(2, "0");
  }

  updateCountdown();
  setInterval(updateCountdown, 1000);
</script>

<style>
  .hero {
    position: relative;

    min-height: 100svh;

    display: flex;
    align-items: center;
    justify-content: center;

    padding: 7rem 1.5rem 3rem;

    overflow: hidden;

    background:
      radial-gradient(
        circle at 50% 20%,
        rgba(234, 220, 197, 0.24),
        transparent 38%
      ),
      var(--ivory);
  }

  .hero__frame {
    position: relative;

    z-index: 2;

    width: min(100%, 1040px);

    padding: clamp(3.5rem, 7vw, 6rem) clamp(2rem, 7vw, 6rem);

    border: 1px solid rgba(191, 154, 89, 0.32);

    border-radius: 22rem 22rem 2rem 2rem;

    text-align: center;
  }

  .hero__frame::before {
    content: "";

    position: absolute;

    inset: 10px;

    pointer-events: none;

    border: 1px solid rgba(104, 133, 116, 0.11);

    border-radius: inherit;
  }

  .hero__content {
    position: relative;

    z-index: 2;

    max-width: 760px;

    margin-inline: auto;
  }

  .hero__kicker {
    margin: 0 0 1.2rem;

    color: var(--terracotta);

    font-size: 0.72rem;
    font-weight: 500;

    letter-spacing: 0.18em;

    text-transform: uppercase;
  }

  .hero__names {
    margin: 0;

    color: var(--green-deep);

    font-family: var(--font-script);

    font-size: clamp(5.2rem, 11vw, 9.5rem);

    font-weight: 400;

    line-height: 0.8;
  }

  .hero__names span {
    display: inline-block;
  }

  .hero__ampersand {
    margin-inline: 0.12em;

    color: var(--terracotta);

    font-size: 0.62em;
  }

  .hero__announcement {
    margin: 1.5rem 0 0;

    color: var(--green);

    font-family: var(--font-serif);

    font-size: clamp(1.5rem, 2.5vw, 2rem);

    font-style: italic;
  }

  .hero__ornament {
    width: min(300px, 70%);

    margin: 2rem auto;

    display: flex;
    align-items: center;

    gap: 1rem;

    color: var(--terracotta);
  }

  .hero__ornament span {
    flex: 1;

    height: 1px;

    background: linear-gradient(
      to right,
      transparent,
      rgba(191, 154, 89, 0.65)
    );
  }

  .hero__ornament span:last-child {
    background: linear-gradient(to left, transparent, rgba(191, 154, 89, 0.65));
  }

  .hero__ornament i {
    font-style: normal;
  }

  .hero__intro {
    max-width: 520px;

    margin: 0 auto;

    color: var(--text-soft);

    font-family: var(--font-serif);

    font-size: clamp(1.25rem, 2vw, 1.55rem);

    line-height: 1.5;
  }

  /* COUNTDOWN */

  .countdown {
    width: min(620px, 100%);

    margin: clamp(2.5rem, 5vw, 3.5rem) auto 0;

    display: grid;

    grid-template-columns: repeat(4, 1fr);

    border-top: 1px solid rgba(191, 154, 89, 0.32);

    border-bottom: 1px solid rgba(191, 154, 89, 0.32);
  }

  .countdown__item {
    position: relative;

    padding: 1.35rem 0.6rem;
  }

  .countdown__item:not(:last-child)::after {
    content: "";

    position: absolute;

    top: 25%;
    right: 0;

    width: 1px;
    height: 50%;

    background: rgba(191, 154, 89, 0.25);
  }

  .countdown__item strong {
    display: block;

    color: var(--green-deep);

    font-family: var(--font-body);

    font-size: clamp(1.8rem, 3.5vw, 2.7rem);

    font-weight: 300;

    line-height: 1;

    letter-spacing: 0.04em;

    font-variant-numeric: tabular-nums;
  }

  .countdown__item span {
    display: block;

    margin-top: 0.6rem;

    color: var(--text-soft);

    font-size: 0.55rem;

    letter-spacing: 0.16em;

    text-transform: uppercase;
  }

  .hero__date {
    margin: 1.8rem 0 0;

    color: var(--terracotta);

    font-size: 0.72rem;
    font-weight: 500;

    letter-spacing: 0.22em;

    text-transform: uppercase;
  }

  /* BUTTONS */

  .hero__actions {
    display: flex;
    justify-content: center;

    flex-wrap: wrap;

    gap: 1rem;

    margin-top: 2rem;
  }

  .hero__button {
    min-height: 50px;

    display: inline-flex;
    align-items: center;
    justify-content: center;

    padding-inline: 1.8rem;

    border-radius: 999px;

    font-size: 0.78rem;

    transition:
      transform 250ms ease,
      background 250ms ease,
      color 250ms ease;
  }

  .hero__button:hover {
    transform: translateY(-2px);
  }

  .hero__button--primary {
    background: var(--terracotta);

    color: white;
  }

  .hero__button--primary:hover {
    background: #c96f59;
  }

  .hero__button--secondary {
    border: 1px solid rgba(64, 101, 86, 0.35);

    color: var(--green-deep);
  }

  .hero__button--secondary:hover {
    background: rgba(104, 133, 116, 0.08);
  }

  /* FLOWERS */

  .hero__flowers {
    position: absolute;

    z-index: 1;

    width: clamp(230px, 28vw, 440px);

    pointer-events: none;

    mix-blend-mode: multiply;
  }

  .hero__flowers--top {
    top: 40px;
    left: -80px;

    transform: rotate(-12deg);

    opacity: 0.65;
  }

  .hero__flowers--bottom {
    right: -80px;
    bottom: -40px;

    transform: rotate(8deg);

    opacity: 0.7;
  }

  @media (max-width: 700px) {
    .hero {
      padding: 6.5rem 1rem 2rem;
    }

    .hero__frame {
      padding: 4rem 1.2rem 3rem;

      border-radius: 10rem 10rem 1.5rem 1.5rem;
    }

    .hero__names {
      font-size: clamp(4.8rem, 22vw, 7rem);

      line-height: 0.82;
    }

    .hero__names span {
      display: block;
    }

    .hero__ampersand {
      margin: 0.08em 0;
    }

    .hero__announcement {
      margin-top: 1rem;
    }

    .countdown__item {
      padding: 1.15rem 0.25rem;
    }

    .countdown__item strong {
      font-size: clamp(1.55rem, 8vw, 2.1rem);
    }

    .countdown__item span {
      font-size: 0.48rem;

      letter-spacing: 0.08em;
    }

    .hero__actions {
      flex-direction: column;
    }

    .hero__button {
      width: 100%;
    }

    .hero__flowers {
      width: 230px;
    }

    .hero__flowers--top {
      top: 70px;
      left: -130px;

      opacity: 0.42;
    }

    .hero__flowers--bottom {
      right: -120px;
      bottom: -30px;

      opacity: 0.48;
    }
  }
</style>
