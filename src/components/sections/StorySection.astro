<section class="story">
  <div class="story__shell">
    <!-- PARTIE FLORALE -->
    <div class="story__visual" aria-hidden="true">
      <div class="story__halo"></div>

      <img src="/images/decorations/deco1.png" alt="" class="story__flowers" />

      <p class="story__monogram">
        A <span>&</span> G
      </p>
    </div>

    <!-- TEXTE -->
    <div class="story__content">
      <p class="story__script">Notre histoire continue…</p>

      <h2>
        Une nouvelle aventure
        <span>commence ici.</span>
      </h2>

      <div class="story__line" aria-hidden="true">
        <span></span>
        <i>♡</i>
        <span></span>
      </div>

      <p class="story__lead">
        Après les voyages, les fous rires, les projets et tous ces petits
        moments qui ont construit notre histoire, il est temps d’en écrire un
        nouveau chapitre.
      </p>

      <p class="story__text">
        Nous avons envie de le vivre entourés des personnes qui comptent pour
        nous. Alors préparez votre bonne humeur, vos plus beaux sourires et vos
        meilleures chaussures de danse.
      </p>

      <p class="story__signature">Anaïs & Glen</p>
    </div>
  </div>

  <!-- PETITE BANDE D'INFOS -->
  <div class="story__details">
    <div class="story__detail">
      <span>Le grand jour</span>
      <strong>19 juin 2027</strong>
    </div>

    <div class="story__separator" aria-hidden="true">✦</div>

    <div class="story__detail">
      <span>Rendez-vous</span>
      <strong>En Provence</strong>
    </div>

    <div class="story__separator" aria-hidden="true">✦</div>

    <div class="story__detail">
      <span>Et ensuite ?</span>
      <strong>On danse jusqu’au bout de la nuit</strong>
    </div>
  </div>
</section>

<style>
  /* ========================================
     SECTION
     ======================================== */

  .story {
    padding: clamp(4.5rem, 8vw, 7rem) 1.5rem clamp(5rem, 9vw, 8rem);

    background: var(--ivory-light);
  }

  /* ========================================
     GRAND PANNEAU
     ======================================== */

  .story__shell {
    width: min(1180px, 100%);

    margin-inline: auto;

    display: grid;

    grid-template-columns:
      minmax(0, 0.9fr)
      minmax(0, 1.1fr);

    min-height: 620px;

    overflow: hidden;

    border-radius: 3rem 10rem 3rem 3rem;

    background: linear-gradient(135deg, #dce5d9 0%, #edf0e8 100%);

    box-shadow: 0 25px 70px rgba(72, 87, 72, 0.07);
  }

  /* ========================================
     VISUEL
     ======================================== */

  .story__visual {
    position: relative;

    min-height: 620px;

    display: flex;
    align-items: center;
    justify-content: center;

    overflow: hidden;
  }

  .story__halo {
    position: absolute;

    width: 430px;
    height: 430px;

    border: 1px solid rgba(191, 154, 89, 0.38);

    border-radius: 50%;
  }

  .story__halo::before {
    content: "";

    position: absolute;

    inset: 18px;

    border: 1px solid rgba(255, 255, 255, 0.55);

    border-radius: 50%;
  }

  .story__flowers {
    position: absolute;

    z-index: 2;

    width: min(520px, 110%);

    left: -7%;
    bottom: -1%;

    mix-blend-mode: multiply;

    filter: saturate(0.9);
  }

  .story__monogram {
    position: absolute;

    z-index: 4;

    top: 49%;
    left: 53%;

    margin: 0;

    color: var(--green-deep);

    font-family: var(--font-script);

    font-size: clamp(4.5rem, 7vw, 6.8rem);

    line-height: 1;

    white-space: nowrap;

    text-shadow:
      0 2px 0 rgba(255, 253, 248, 0.95),
      0 0 18px rgba(255, 253, 248, 0.9),
      0 0 34px rgba(255, 253, 248, 0.75);

    transform: translate(-50%, -50%);
  }

  .story__monogram span {
    color: var(--terracotta);
  }
  /* ========================================
     TEXTE
     ======================================== */

  .story__content {
    display: flex;
    flex-direction: column;
    justify-content: center;

    padding: clamp(3rem, 7vw, 6rem);

    background: rgba(255, 253, 248, 0.88);
  }

  .story__script {
    margin: 0 0 0.7rem;

    color: var(--terracotta);

    font-family: var(--font-script);

    font-size: clamp(2rem, 3vw, 2.8rem);
  }

  .story__content h2 {
    margin: 0;

    color: var(--green-deep);

    font-family: var(--font-serif);

    font-size: clamp(3rem, 5vw, 5rem);

    font-weight: 400;

    line-height: 0.98;
  }

  .story__content h2 span {
    display: block;

    color: var(--terracotta);
  }

  /* ========================================
     PETIT ORNEMENT
     ======================================== */

  .story__line {
    width: min(280px, 70%);

    margin: 2rem 0;

    display: flex;
    align-items: center;

    gap: 0.9rem;

    color: var(--gold);
  }

  .story__line span {
    flex: 1;

    height: 1px;

    background: rgba(191, 154, 89, 0.45);
  }

  .story__line i {
    color: var(--terracotta);

    font-style: normal;

    font-size: 0.85rem;
  }

  /* ========================================
     PARAGRAPHES
     ======================================== */

  .story__lead {
    max-width: 560px;

    margin: 0;

    color: var(--ink);

    font-family: var(--font-serif);

    font-size: clamp(1.4rem, 2vw, 1.75rem);

    line-height: 1.5;
  }

  .story__text {
    max-width: 540px;

    margin: 1.5rem 0 0;

    color: var(--text-soft);

    font-size: 0.95rem;

    line-height: 1.85;
  }

  .story__signature {
    margin: 1.8rem 0 0;

    color: var(--green-deep);

    font-family: var(--font-script);

    font-size: 2.3rem;
  }

  /* ========================================
     INFOS
     ======================================== */

  .story__details {
    width: min(1000px, calc(100% - 2rem));

    margin: clamp(3rem, 5vw, 4.5rem) auto 0;

    display: grid;

    grid-template-columns:
      1fr
      auto
      1fr
      auto
      1.3fr;

    align-items: center;

    gap: clamp(1rem, 3vw, 2.5rem);

    text-align: center;
  }

  .story__detail span {
    display: block;

    margin-bottom: 0.45rem;

    color: var(--terracotta);

    font-size: 0.58rem;
    font-weight: 500;

    letter-spacing: 0.18em;

    text-transform: uppercase;
  }

  .story__detail strong {
    display: block;

    color: var(--green-deep);

    font-family: var(--font-serif);

    font-size: clamp(1.3rem, 2vw, 1.7rem);

    font-weight: 400;

    line-height: 1.2;
  }

  .story__separator {
    color: var(--gold);

    font-size: 0.75rem;
  }

  /* ========================================
     TABLETTE
     ======================================== */

  @media (max-width: 900px) {
    .story__shell {
      grid-template-columns: 0.8fr 1.2fr;
    }

    .story__content {
      padding: 3rem 2.5rem;
    }

    .story__visual,
    .story__shell {
      min-height: 560px;
    }
  }

  /* ========================================
     MOBILE
     ======================================== */

  @media (max-width: 700px) {
    .story {
      padding: 3.5rem 1rem 5rem;
    }

    .story__shell {
      display: flex;
      flex-direction: column;

      min-height: 0;

      border-radius: 2rem 5rem 2rem 2rem;
    }

    .story__visual {
      min-height: 330px;
    }

    .story__halo {
      width: 270px;
      height: 270px;
    }

    .story__flowers {
      width: 340px;

      left: -78px;
      bottom: -38px;
    }

    .story__monogram {
      top: 48%;
      left: 56%;

      font-size: clamp(4.6rem, 20vw, 6rem);

      transform: translate(-50%, -50%);
    }

    .story__content {
      padding: 3rem 1.6rem 3.5rem;

      text-align: center;
    }

    .story__script {
      font-size: 2rem;
    }

    .story__content h2 {
      font-size: clamp(2.8rem, 13vw, 3.8rem);
    }

    .story__line {
      margin: 1.7rem auto;
    }

    .story__lead {
      margin-inline: auto;

      font-size: 1.45rem;
    }

    .story__text {
      margin: 1.5rem auto 0;
    }

    .story__signature {
      margin-top: 1.8rem;
    }

    .story__details {
      display: flex;
      flex-direction: column;

      width: 100%;

      gap: 0;

      margin-top: 2.8rem;

      padding-inline: 0.5rem;
    }

    .story__detail {
      width: 100%;

      padding: 1.5rem 0;

      border-bottom: 1px solid rgba(191, 154, 89, 0.23);
    }

    .story__detail:first-child {
      border-top: 1px solid rgba(191, 154, 89, 0.23);
    }

    .story__separator {
      display: none;
    }

    .story__detail strong {
      font-size: 1.55rem;
    }
  }
</style>
